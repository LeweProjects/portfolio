/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    env: {
        URL: "portfolio-1v09pj617-leweprojects-projects.vercel.app",
      }
};

export default nextConfig;
