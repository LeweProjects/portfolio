import React, { Suspense } from "react";

const page = async () => {
  return (
    <div className="flex w-full h-screen justify-center items-center">
      Profile
    </div>
  );
};

export default page;
